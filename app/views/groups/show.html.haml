%p
  %strong Group Name:
  = @groups.group_name
%p
  %strong Description:
  = @groups.group_description
  
%h2 Transactions
= render @groups.transactions

%h2 Add a transaction:
= render 'transactions/form'

= link_to 'Home', groups_path
= link_to 'Edit', edit_group_path(@groups)