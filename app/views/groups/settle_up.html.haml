%div#body
    .topnav
        <img src="https://img.icons8.com/cotton/50/000000/money-transfer.png" alt="money logo" class="images"/>
        DinDiv
        = link_to 'Back', group_path(@group)
    %div#settleUp
        - @settle.each do |owes_user, owed_users|
            - owed_users.each do |user, amount|
                = owes_user.name
                owes
                = user.name
                = number_to_currency(amount)
            